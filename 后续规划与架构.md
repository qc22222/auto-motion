# 后续进展规划与架构(第一性原理版)

> 更新时间:2026-08-03
> 用途:交给 DeepSeek(或其他 AI/工程师)作为修改和推进项目的唯一权威依据。
> 阅读顺序:先读第 1、2 章建立世界观,再读第 3 章架构,改代码前必须读第 4 章红线,最后按第 5 章路线图执行。
> 配套文档:`.codex/PROJECT_MEMORY.md`(环境事实)、`video-harness/docs/完整视频工作流与IndexTTS2部署报告.md`(音频细节)、`video-harness/docs/用户验收清单.md`(验收步骤)。

---

## 1. 第一性原理:这个项目到底在解决什么问题

### 1.1 终极目标

**一个人,在一台 Windows 笔记本上,用自己的声音,批量生产可发布的竖屏知识/解说类短视频,且每个环节可检查、可修改、可局部返工。**

从这个目标出发,倒推出五个不可回避的基本问题,整个系统的每一个组件都必须能回答"我在解决哪个基本问题":

| # | 基本问题 | 推论 | 对应子系统 |
|---|---|---|---|
| P1 | 内容是否正确? | 文案必须先锁定为唯一事实来源,否则声音、分镜、字幕全部反复返工 | `script.json` + `script` 质量门 |
| P2 | 时间从哪里来? | 镜头时长必须来自真实配音的实测时长,不能先估时再硬塞音频 | TTS → `audio_meta.json` → Storyboard |
| P3 | 中间结果能否修改? | 每个参数影响范围不同,"改一处重跑全部"不可接受,必须有精确失效传播 | 状态机 + 依赖失效 + 编辑工作台 |
| P4 | 失败能否被发现? | "文件存在"不等于"成功",必须逐项验证授权、CUDA、时长、静音、对齐、响度、封装 | 生产预检 + 各阶段质量门 |
| P5 | 昂贵步骤能否复用? | 模型加载一次、旁白按段缓存、镜头按场景返工、字幕独立失效 | 内容寻址缓存 + 单场景返工 |

### 1.2 三条环境公理(不可假设不存在)

这台机器的现实约束,所有代码必须无条件兼容:

- **公理 A:工作区在中文路径**(`E:\自媒体\视频制作`),用户名也是中文(`C:\Users\秦川`)。Python 的 site 模块、sentencepiece、kaldifst、whisper-cli 等原生库用 ANSI(GBK)文件 API,**遇中文路径必然失败**。对策已固化(见 3.6),任何新引入的原生依赖都必须先过中文路径测试。
- **公理 B:只有一块 8 GiB 显存的 RTX 4060 Laptop**。只做推理不训练;禁用 DeepSpeed/CUDA 内核/torch.compile;串行推理不并行;CUDA 不可用时硬失败,**禁止静默回退 CPU**。
- **公理 C:一切项目本地化**。不安装全局依赖、不写用户级技能目录、凭据只进项目 `.env`、缓存固定在 `.codex/` 下。唯一例外:模型文件放 `E:/models`(因公理 A 被迫迁出,且体积大应跨项目共享)。

### 1.3 判断任何改动好坏的唯一标准

> 一个改动如果让 P1–P5 中任何一个问题的答案变得更模糊(比如让"文件存在"重新等价于"成功",或让某个修改的失效范围变成"全部重跑"),它就是退化,无论代码多漂亮都不能合并。

---

## 2. 现状快照(2026-08-03)

### 2.1 已完成且验证过

| 能力 | 验证方式 | 状态 |
|---|---|---|
| SRT → 分镜 → Claude Code 逐镜头 MG 动画 → 拼接(auto-motion 原始链路) | `runs/2026-08-01_codex门槛测试` 有成片 | ✅ |
| Video Harness 状态机、审批、编辑工作台、字幕、渲染、交付 | 41 项自动测试全绿 | ✅ |
| 模拟模式端到端(静音 TTS + 真实 HyperFrames 渲染 + 成片) | `runs/video-harness-workflow-demo-20260802/delivery/final.mp4` | ✅ |
| IndexTTS2 本地 GPU 推理链路 | 冒烟:一句中文 35–45 秒推理,输出可播放、响度正常 | ✅ |
| whisper.cpp 词级对齐 | 冒烟:转写与输入逐字一致 | ✅ |
| 中文路径四炸弹的代码级对策 | 音频技能 55 项测试全绿 + `-VerifyOnly` 端到端通过 | ✅ |
| 模型资产迁移到 `E:/models` | 按 `model-manifest.json` 逐文件校验 27/27 | ✅ |
| Git 入库与工程治理(P1) | 6+2 commits,推送到 fork,工作区干净 | ✅ |
| kokoro 本地默认音色端到端成片(真实音频) | `runs/video-demo-20260803/delivery/final.mp4` 13.2s,3 场景,字幕/母带齐全 | ✅ |
| IndexTTS2 克隆音色真实合成(用户参考音频) | `audio` 真实合成 11.47s,用户试听确认音色 | ✅ |
| kokoro 引擎链路修复(24kHz 转码、espeak-ng ASCII 数据、setup 脚本兼容) | 音频技能 55 项全绿 + 成片验证(commit f764f62) | ✅ |
| 场景生成并行化(有界并发+失败隔离) | 45 项测试全绿;实测 3 场景 22min vs 串行 50min,成功率/时长零折扣 | ✅ |
| 克隆音色端到端成片(IndexTTS2 + 并行场景 + 真实交付) | uns/video-demo-20260803/delivery/final.mp4 11.5s,用户确认质量 | ✅ |

### 2.2 未完成(按阻塞程度排序)

1. **whisper 对齐 words 粒度为整句**:transcribe 只输出整句 1 个词时间戳,字幕无法逐词高亮/断句;需在 hyperframes transcribe 或 harness 对齐层修复(whisper tokens 已含逐字时间戳,聚合即可)。
2. **`delivery`/`direction`/`settings`/`pronunciations` 字段部分未消费**:已进契约、预检会如实标注"未消费",但尚未全部映射到 TTS 引擎参数。
3. **OpenDesign 前端接线(P2)**:三页面静态原型已评审、适配度高;待接 `server.mjs` 真实 API(状态/预览/应用/审批/轮询),并补多项目列表页。
4. **正式视频项目**:目前成片都在 `runs/` 演示项目;正式片按文档约定放 `E:/自媒体/01_视频项目/<年份>/`,走完整三门审批。

---

## 3. 系统架构

### 3.1 总览:两代系统的关系

```text
┌─────────────────────────────────────────────────────────────┐
│  第一代:auto-motion(保留,作为"场景视觉生成器"被封装)      │
│  transcription.srt → Codex 拆镜 → Claude Code 逐镜头 →      │
│  HyperFrames 渲染 → ffmpeg 拼接                              │
└──────────────────────────┬──────────────────────────────────┘
                           │ 视觉创作职责原样保留,被下层调用
┌──────────────────────────▼──────────────────────────────────┐
│  第二代:video-harness(产品内核,控制层)                    │
│  文案锁定 → 真实 TTS → 词级对齐 → 音频驱动分镜 →             │
│  场景工程生成(调用第一代能力)→ 审阅/批注/局部返工 →         │
│  渲染 → 混音/母带 → 字幕 → 交付质量门 → final.mp4            │
└─────────────────────────────────────────────────────────────┘
```

**关键决策:harness 不替换视觉生成逻辑,只在外面加输入契约、日志、验收和断点续接。** 原因(第一性):视觉创意是 Claude Code + HyperFrames 技能的强项,把它模板化会毁掉产出质量;而它缺的恰恰是状态管理和可修改性,这正是 harness 补的。

### 3.2 分层架构

```text
┌────────────────────────────────────────────────────────┐
│ 交互层  reviews/index.html + edit.html(本地工作台)     │
│         [未来] OpenDesign 三页面                        │
├────────────────────────────────────────────────────────┤
│ 服务层  lib/server.mjs(仅 127.0.0.1,同源校验,403/409)│
│         lib/job-manager.mjs(异步任务,互斥)            │
├────────────────────────────────────────────────────────┤
│ 控制层  lib/pipeline.mjs(advance 状态机推进)           │
│         lib/state.mjs(阶段状态)lib/model.mjs(事实源)│
│         lib/editor.mjs(预览影响→确认应用→精确失效)    │
├────────────────────────────────────────────────────────┤
│ 生成层  lib/audio.mjs(TTS 编排)lib/captions.mjs       │
│         lib/scenes.mjs + lib/generator.mjs(场景工程)   │
│         lib/render.mjs  lib/assemble.mjs(混音+封装)    │
├────────────────────────────────────────────────────────┤
│ 质检层  lib/audio-readiness.mjs(生产预检)              │
│         lib/validate.mjs  lib/audio-mastering.mjs(响度)│
├────────────────────────────────────────────────────────┤
│ 引擎层  exampleFolder/.claude/skills/media-use/audio/    │
│           scripts/lib/{tts,indextts2,heygen}.mjs         │
│         IndexTTS2(E:/models + .codex/runtime)           │
│         whisper.cpp  HyperFrames  FFmpeg  Claude Code    │
└────────────────────────────────────────────────────────┘
```

### 3.3 数据流与事实来源

**四个可编辑事实来源(用户/工作台只改这四个 JSON,其余全是派生物):**

```text
script.json ──────┐ (P1:内容唯一事实源)
voice-profile.json┤ (音色、提供方、语速、发音表)
design.json ──────┤ (颜色、字体、版式、字幕全局样式)
storyboard.json ──┘ (场景语义、extraHold、视觉重点、转场)
        │ compile / audio / plan / captions / build ...
        ▼
派生物:BRIEF.md、SCRIPT.md、frame.md、STORYBOARD.md、
        audio_meta.json(分段WAV+词级时间戳+场景偏移)、
        captions/(JSON/SRT/VTT/ASS)、scenes/<id>/(独立工程)、
        reviews/、delivery/final.mp4
状态:  .harness/state.json(状态机)、history.jsonl(审计)、
        edit-history.jsonl(修改审计,只存哈希不存值)
```

**时间轴的因果链(P2 的实现,不可倒置):**

```text
script.json 分段文本
  → IndexTTS2 逐段合成(按段内容寻址缓存)
  → 每段实测 duration_s + whisper 词级时间戳
  → 段落归属场景 + extraHold ⇒ 场景真实时长
  → Storyboard 时间轴 ⇒ 场景工程的动画时长
  → 字幕起止时间(来自词级时间戳,非估算)
  → 旁白装配(补齐静音段)⇒ 总时长 ≡ Storyboard 总时长
```

### 3.4 状态机与质量门

阶段序列:`script → audio → storyboard(plan) → captions → scenes(build/generate) → review → render → delivery(assemble)`

- 三个人工质量门:**`script`、`storyboard`、`review`**(automation 模式只留 `review`)。批准动作绝不隐式改参数。
- `advance` 是幂等推进器:自动执行确定性阶段,在质量门暂停;批准后重复运行同一命令续接。
- 失效传播规则(P3 的核心,`lib/model.mjs`/`editor.mjs`):

| 修改 | 失效范围 |
|---|---|
| 旁白文本/演绎/停顿 | 音频(仅变化段)+ 分镜 + 镜头 + 字幕 + 审阅 + 交付,撤销三门审批 |
| 普通分镜视觉参数 | 仅目标镜头 + 下游 |
| `extraHold` | 目标镜头 + 音频静音段 + 后续字幕时间轴 + 交付 |
| 单镜头画面计划/批注 | 仅该镜头 + 审阅 + 渲染 + 交付 |
| 单条字幕 | 仅字幕 + 审阅 + 渲染 + 交付(**不**碰音频和镜头) |
| 是否烧录字幕 | 仅最终交付 |

### 3.5 音频引擎架构

```text
lib/audio.mjs(harness 侧编排)
  → 生产预检 audio-readiness.mjs:凭据/音色/CUDA/对齐能力/BGM 依赖,
     任何一项不过 → 在调用外部服务前失败(P4)
  → 引擎脚本 media-use/audio/scripts/audio.mjs
      ├─ heygen.mjs   (云端,消费原生词级时间戳)
      ├─ kokoro       (本地轻量,无克隆,zh-CN→zh 规范化)
      └─ indextts2.mjs(本地 GPU 克隆音色,当前主路线)
            批量 JSONL → python -S -m indextts.cli_v2 batch
            → 一次加载模型,串行合成同批缺失段(P5)
            → ffmpeg atempo 调速(0.5–2.0)→ 重新词级对齐
  → 缓存:.harness/audio-cache/tts/ 按段内容寻址;只重合成变化段
  → BGM:异步生成,bgm_pending=true 时硬性阻止最终装配
  → 母带:旁白 -16 LUFS,混音 -14 LUFS,真峰值 ≤ -1 dBTP,不达标交付失败
```

### 3.6 中文路径兼容层(公理 A 的固化对策)

| 炸弹 | 位置 | 已固化对策 |
|---|---|---|
| Python site 读 `.pth`(GBK) | venv 启动 | 一律 `python -S` + 显式 `PYTHONPATH` |
| kaldifst 读 `.fst`(ANSI) | wetext 文本规范化 | `indextts2.mjs#ensureAsciiExecRoot()`:推理前自动 `subst` 建 ASCII 盘符,重启自愈 |
| sentencepiece 读 `bpe.model`,且 CLI `Path.resolve()` 会还原 subst 盘符 | 模型加载 | 模型目录固定 `E:/models/indextts2`(三处默认值同步:`audio-paths.mjs`、`indextts2.mjs`、`setup-indextts2.ps1`) |
| whisper-cli 读中文 `%USERPROFILE%` 下模型 | HyperFrames transcribe | `tts.mjs`:对齐子进程 `USERPROFILE` 重定向到 `E:/models/hyperframes-home`(模型硬链接,单份) |
| HyperFrames 原生 `init` GBK 乱码 | 工程脚手架 | 禁止 `npx hyperframes init`,统一走 `hyperframes-local.ps1` |

**规则:今后任何新增的原生二进制依赖,合并前必须回答"它用什么编码打开文件路径",并补一条中文路径测试。**

---

## 4. 不可破坏的契约(DeepSeek 修改红线)

违反任何一条即视为破坏性修改,必须先与用户确认:

1. **四个 JSON 是唯一可编辑事实来源**;禁止让任何派生物(SRT、audio_meta、渲染产物)反向改写它们;禁止 ASR 反向改文案。
2. **时长因果链不可倒置**:永远是真实音频决定分镜时长,不允许"先定时长再拉伸音频"重新出现(atempo 调速属于合成内部环节,且调速后必须重新对齐)。
3. **质量门语义**:只有 `script`/`storyboard`/`review` 三门可批准;批准不改参数;修改会按 3.4 的表撤销对应审批,不能"悄悄保留"。
4. **失效范围只能更精确,不能更粗**:任何重构不得把"改一条字幕"退化为"重跑音频"。
5. **生产/模拟硬隔离**:`mock` 产物必须显式标记;生产校验和交付默认拒绝 mock 音频。
6. **CUDA 硬失败**:禁止任何形式的 CPU 推理回退。
7. **安全边界**:参考音频必须在项目目录内(拒绝绝对路径/目录穿越);未勾选授权不合成;凭据只进 `.env`;服务只监听 127.0.0.1、拒绝非同源、长任务期间写入返回 409;`edit-history.jsonl` 只记哈希不记值。
8. **本地化**:不新增全局安装、全局技能、用户级缓存(公理 C 及其唯一例外)。
9. **模型资产路径**:`E:/models` 下的布局与 `model-manifest.json` 固定版本策略不可改;升级模型必须走"新版本目录 + 清单更新",不原地覆盖。
10. **测试基线只增不减**:当前 harness 41 项 + 音频技能 55 项 + `auto-test/hyperframes-local.test.ps1` 全绿;每个行为改动配套测试;测试不得依赖网络或真实 GPU(GPU 路径用注入 mock,参考 `indextts2.test.mjs` 的 `deps.spawn` 模式)。
11. **执行契约**:`run-claude-ai.sh` 的非交互调用、`[[USER_MESSAGE]]` 阶段汇报、日志三件套(`stream.jsonl`/`stderr.log`/`user.log`)格式不可变——上游 Codex 调度依赖它们。

---

## 5. 后续路线图

依赖关系:P0 与 P1 并行,P2 依赖 P1,P3 依赖 P0,P4 依赖 P0+P3,P5 持续进行。

### P0(本周)真实音色验收 —— 唯一带外部依赖的关键路径

> **状态(2026-08-03 晚):已完成。克隆音色端到端成片已产出(uns/video-demo-20260803/delivery/final.mp4,11.5s)并获用户确认。剩余小项:词级时间戳粒度修复(见 2.2 第 2 条)。**

- **目标**:用用户本人授权音色跑通 `audio-check → audio`,产出可试听的真实旁白。
- 任务:
  1. 用户按 `参考音频录制指南.md` 录 8–15 秒参考音频,放入视频项目 `assets/reference/`。
  2. 工作台选 IndexTTS2、填参考音频相对路径、勾授权确认。
  3. `audio-check` 逐项排预检 → `audio`(真实,无 `--mock`)。
  4. 试听;必要时 `--force-tts-line` 单段重合成;验证 `audio_meta.json` 每段有非空 `words`。
- **验收**:分段 WAV 与 `narration.wav` 可播放且非静音;词级时间戳完整;总时长与 Storyboard 一致误差 <0.1s;多音字可用发音表修正。
- 风险:首次真实长文本可能暴露 8 GiB 显存不足 → 对策:串行 + 分段本就是默认;若单段过长 OOM,在 compile 阶段限制单段字数并给出明确报错。

### P1(本周,与 P0 并行)Git 入库与工程治理

- **目标**:消除"一切都悬在工作区"的风险。
- 任务(建议分批 commit,每批可独立回滚):
  1. `chore: skills 重组`(hyperframes 技能目录拆分 + 备份说明);
  2. `feat: video-harness 内核`(lib/bin/tests/docs);
  3. `feat: indextts2 本地音频链路`(引擎脚本 + setup 工具);
  4. `fix: 中文路径兼容层`(本次四炸弹对策);
  5. `docs: 交接与规划`(本文档、PROJECT_MEMORY、录音指南)。
  6. 补 `.gitignore`:确认 `runs/`、`.codex/runtime`、`.codex/models` 引用、`*.smoke`、`assets/reference/` 均被忽略;**逐项检查不把参考音频、`.env`、模型权重误提交**。
- **验收**:`git status` 干净;每个 commit 信息说清动机;新克隆 + `setup-indextts2.ps1` 可从零复原环境。

### P2(1–2 周)OpenDesign 前端接入

- **目标**:把三页静态原型接到 harness 真实数据,替换硬编码。
- **架构原则(不可违背)**:前端是 `server.mjs` 已有 JSON 接口的消费者,**不在前端复制状态机**;推进/批准/生成都走既有异步任务接口轮询,前端只渲染状态。
- 任务:
  1. 盘点三页面所需数据字段 → 映射到现有接口;缺口只允许在服务层补只读端点,不开新的写路径(写操作一律走 editor 的"预览影响→确认应用")。
  2. 逐页替换硬编码:项目列表页 → 扫描 `01_视频项目`;项目详情页 → `state.json` + 各产物;审阅页 → 复用现有 reviews/edit 能力。
  3. 浏览器可视化走查(工作台点击、409/403 行为、响应式)。
- **验收**:三页面全部数据来自接口;关闭 harness 服务时前端能明确显示不可用而非假数据;`server.test.mjs` 扩展覆盖新端点。

### P3(2–3 周)第一支真实成片(端到端串联)

- **目标**:选一个真实选题,从文案到 `delivery/final.mp4` 全真实(真音色、真场景生成、真渲染、真母带),发布到平台。
- 任务:
  1. 新建正式项目于 `E:/自媒体/01_视频项目/2026/…`;
  2. 走完整 `advance` 流程,三门人工审;
  3. 记录每阶段耗时与人工干预点 → 写入 `runs/` 复盘文档;
  4. 期间暴露的问题按红线修复并补测试。
- **验收**:成片通过时长/分辨率/双流/响度全部生产校验;`用户验收清单.md` A–H 逐项打勾;发布后归档到 `03_发布管理`。

### P4(3–6 周)演绎参数全量落地 + 效率优化

- **目标**:消灭"字段已填写≠演绎已生效"的灰区;把单片生产时间压下来。
- 任务:
  1. 逐项映射 `direction`/逐段 `delivery`/`settings`/`pronunciations` 到 IndexTTS2 参数(情绪文本/八维向量/权重);每映射一项,从预检"未消费"清单里划掉一项;映射不了的保持显式"未消费"标注,**禁止假装生效**。
  2. 发音表:落实多音字/英文/数字读法替换发生在送入 TTS 前的文本层,且不污染字幕文本(字幕显示原文)。
  3. 效率:统计 P3 复盘中的耗时大头;候选优化——TTS 批内复用已加载模型(已有)、场景生成的提示词模板沉淀、渲染并发度(受显存约束,谨慎)。
  4. 复盘 auto-motion 与 harness 的双入口关系:若 harness 全面稳定,评估将 `PROMPT.md` 入口标注为 legacy(只标注,不删除)。
- **验收**:预检"未消费"清单为空或每项有明确理由;单片(60–90 秒成片)从锁稿到交付的无人值守时间有量化基线和改进数字。

### P5(持续)工程健康

- 每次会话结束:改动入库、测试全绿、`PROJECT_MEMORY.md` 增量更新。
- 每引入一个新原生依赖:中文路径测试 + 固定版本 + 项目本地安装三连。
- 每月:`workspace_clean` 清理崩溃转储;核对 `E:/models` 清单;检查 `.codex` 缓存体积。
- 文档漂移检查:README 中的数字(测试数、技能数)与实际对齐。

---

## 6. 给 DeepSeek 的操作指引

1. **改前必读**:第 4 章红线 + `.codex/PROJECT_MEMORY.md`。对任何"为什么这么绕"的代码(subst、`-S`、USERPROFILE 重定向),先假设它在踩过的坑上,查本文件 3.6 节,不要"顺手简化"。
2. **改动流程**:定位阶段(3.2 分层)→ 确认失效传播影响(3.4 表)→ 写/改测试 → 实现 → 跑三套测试(`video-harness` npm test、音频技能 `node --test lib/*.test.mjs`、`auto-test/hyperframes-local.test.ps1`)→ 更新相关文档。
3. **测试约定**:不联网、不动真实 GPU、不写用户目录;外部进程一律走 `deps.spawn`/`deps.spawnSync` 注入 mock;临时目录注意本机 tmpdir 也在中文路径下(这本身就是测试价值)。
4. **禁止事项速查**:不 `npx hyperframes init`;不 `hyperframes skills`;不全局安装;不删 `[[USER_MESSAGE]]` 协议;不让 mock 产物混进生产交付;不原地覆盖 `E:/models` 模型文件。
5. **提问优先**:凡涉及红线的取舍、需要用户授权的资源(音色、凭据、发布账号),停下来向用户确认,不要替用户决定。
